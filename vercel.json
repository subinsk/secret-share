{
  "version": 2,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/web/$1"
    }
  ],
  "installCommand": "yarn install",
  "buildCommand": "prisma generate && yarn build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "env": {
    "NODE_VERSION": "18.x"
  }
}
